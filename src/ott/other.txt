驱动里面的内存分配， 一个29M的 Heap 管理这个heap 需要 kmalloc 2k 空间， 智能管理70
多个 block（一个内存区域）但是，分配一个filter就要用掉2个block，而分配多个fileter是
由用户（DM的开发者）决定的。 最后把2k改成4k结果就可以分配140个block，就解决了。所以某些问题到项目后期才能暴露。

原厂发布包的拆分（便于编译和管理）
发布时候，是一站式编译的。利于发布但不利于开发。

刚看到xbmc的json rpc时，觉得很好。后发现不实用
网关设置web页的设计，开始学习js+css+html 想借用google的页面设计效果后来妥协，选择极路由。

OTT码流服务器，内外网访问，开发测试环境，经常出问题，其实都很简单，只是各个简单的环节
都没有做扎实，一堆不扎实的东西堆在一起就是一堆垃圾 扎扎实实的做好每步，看起来慢，实际上是快。

天和版本
 RC_Server.apk 是手机遥控器盒子端的接收服务。 手机端程序天和给了源码：RemoteIME 
（表跟输入法那个程序混淆了，名字一样）


http://tools.ietf.org/html/draft-zyp-json-schema-03#section-5.16

section-5.16 指定到具体章节


1 对接生产工具，工厂模式（检测usb，网络，视频输出等）
2 杜比库
3 生产用image制作 （slc  mlc）


datadroid控件 页面数据缓冲
